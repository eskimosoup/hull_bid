<% title DefaultGalleryImage.model_name.human.pluralize %>

<div class="admin_functions">
  <%= link_to "New #{ DefaultGalleryImage.model_name.human }", new_admin_default_gallery_image_path %>
</div>

<%= informant_table @default_gallery_images, {
      :headings => [ 'Name', 'Display', '', '', '' ],
      :body => [ :name, it_ajax_toggle(:display), it_edit_images(:image), it_edit, it_destroy ]
    } %>
