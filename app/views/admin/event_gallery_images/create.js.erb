<% if @event_gallery_image.new_record? %>
  alert("Failed to upload image: <%= j @event_gallery_image.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#images").append("<%= j render(@event_gallery_image) %>");
<% end %>