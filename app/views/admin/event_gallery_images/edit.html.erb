<%- title "Edit #{ EventGalleryImage.model_name.human }" %>

<div class="admin_functions">
  <%= link_to 'Back', admin_event_gallery_event_gallery_images_path(@event_gallery) %>
</div>

<%= render 'form' %>
