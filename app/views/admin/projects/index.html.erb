<% title Project.model_name.human.pluralize %>

<div class="admin_functions">
  <%= link_to "New #{ Project.model_name.human }", new_admin_project_path %>
</div>

<%= render 'admin/shared/search' %>

<%= informant_table @projects, {
      :headings => [ :title,  :display, '', '', '' ], 
      :body => [ :title, it_ajax_toggle(:display), it_edit_images(:image_1), it_edit, it_destroy ]
    } %>

<%= paginate @projects %>
